'use strict';

describe('sher.task module', function() {

  beforeEach(module('sher.task'));

  describe('task controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('TaskCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});